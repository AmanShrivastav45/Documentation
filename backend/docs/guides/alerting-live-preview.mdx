# Alerting Live Preview

The panel below renders a live-style metric gauge so you can see how an
alert threshold behaves against recent data, without leaving the docs page.

<LiveMetricPreview threshold={80} />

When the latest value crosses the threshold, the gauge switches from green
to red — the same visual language used in the alerting UI itself.
